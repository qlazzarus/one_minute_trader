# Sprite Assets Placeholder

Place Bibi character sprites here:

- `bibi_idle.png`
- `bibi_talk.png`
- `bibi_happy.png`
- `bibi_sad.png`

Each image should share the same resolution and transparent background so texture swaps remain seamless. Phaser loads them in `BootScene.preload()` via the `assets/sprites/` path.
